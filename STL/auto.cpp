#include<iostream>

using namespace std;

int main(){
  // auto NPCname = "BurgerKing";
  // std::cout << NPCname << std::endl;
  // auto Number = 1;
  // std::cout << Number << std::endl;

  // int UserMode = 4;
  // auto* pUserMode = &UserMode; // Save Pointer of UserMode to pUserMode
  // cout << *pUserMode << endl;
  // auto& refUserMode = UserMode;  //refUserMode is Reference of UserMode
  // refUserMode = 5;             //change of UserMode
  // cout << UserMode << endl;

  // int NumberList[5] = {1,2,3,4,5};
  // for(auto i: NumberList)         //순서대로 NumberList에서 하나씩 꺼낸다.
  //   cout << i << endl;





  return 0;
}
