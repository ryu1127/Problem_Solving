#include<stdio.h>
#include<vector>

using namespace std;
int count = 0;
int sc[20];
int temp[20];
int hap = 0;
void DFS(int hap,int first) {
	hap = hap + first;
	
	
}

int main() {
	int n, s;
	scanf("%d%d", &n, &s);
	

	for (int i = 0; i < n; i++) {
		int temp;
		scanf("%d", &temp);
	}
}